package rule
import org.apache.spark.sql.Dataset
import org.apache.spark.sql.Row

//   Check HTTP in column ?
class CheckHTTP {
  def checkingRequestType(newDataFrame: Dataset[Row], oldDataFrame: Dataset[Row]){
     
  }
}